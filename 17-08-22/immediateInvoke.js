// Immediate invoking of function expresson (iife)
// wrap all this in parenthesisi and put ()

(
(a,b)=>{
    // api
    //call db
    // call fs
    console.log("hello World =>",a+b);
}
) (2,3); // calling once;